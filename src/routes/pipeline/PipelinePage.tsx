import { ListChecks } from 'lucide-react'

export function PipelinePage() {
  return (
    <div className="route-placeholder">
      <div className="route-placeholder-content">
        <ListChecks size={48} strokeWidth={1} />
        <h1>Pipeline</h1>
        <p>Job search tracker — coming soon.</p>
        <p className="route-placeholder-hint">
          Track applications, paste JDs, and launch into Facet&apos;s build view with
          vector-matched positioning.
        </p>
      </div>
    </div>
  )
}
